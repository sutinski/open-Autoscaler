{
	"services": [{
		"name": "${service.name}",
		"id": "${service.name}-id",
		"version": "1.0",
		"bindable": true,
		"description": "Automatically increase or decrease the number of application instances based on a policy you define.",
		"plans": [{
			"id": "${service.name}-default-plan",
			"name": "default",
			"description": "This is the default service plan for the Auto-Scaling service."
		}]
	}]
}
